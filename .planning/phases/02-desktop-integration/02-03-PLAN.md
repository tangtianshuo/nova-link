---
phase: 02-desktop-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/composables/useGreeting.ts
  - src/App.vue
  - src/components/CharacterSettingsModal.vue
autonomous: true
requirements:
  - GREETING-01

must_haves:
  truths:
    - 用户可以设置定时提醒/问候
    - 到设定时间时应用主动发送消息
    - 用户可以启用/禁用定时功能
  artifacts:
    - path: src/composables/useGreeting.ts
      exports: useGreeting
    - path: CharacterSettingsModal.vue
      contains: greeting settings UI
  key_links:
    - from: useGreeting
      to: useChat
      via: onGreetingTriggered callback
    - from: useGreeting
      to: useNotification
      via: notify function
---

# Plan: 02-desktop-integration

**Phase:** 2
**Plan:** 3
**Type:** execute
**Wave:** 2
**Goal:** 实现定时问候功能 - 主动发送消息

## Context

### User Decisions (from CONTEXT.md)
- 使用通知提醒用户
- 定时任务存储在 settings 中
- 问候消息支持自定义

### Must-Haves (Goal-Backward)

**Truths:**
1. 用户可以设置定时提醒/问候
2. 到设定时间时应用主动发送消息
3. 用户可以启用/禁用定时功能

**Artifacts:**
- `src/composables/useGreeting.ts` - 定时问候管理
- `src/composables/useNotification.ts` - 通知提醒（已在 Plan 1 创建）
- 设置中的定时问候UI

**Key Links:**
- useGreeting -> setInterval -> 定时检查
- 到达设定时间 -> 发送消息到聊天
- useNotification -> 发送 Windows 通知

---

## Tasks

### Task 1: 创建定时问候Composable

**requirements:** GREETING-01

**files_modified:**
- `src/composables/useGreeting.ts` (新建)

**<read_first>**
- `src/composables/useChat.ts` - 查看消息发送方式
- `src/composables/useSettings.ts` - 参考现有设置存储

**</read_first>**

**<action>**

创建 `src/composables/useGreeting.ts`：
```typescript
import { ref, onUnmounted } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import { useNotification } from './useNotification'

export interface GreetingConfig {
  enabled: boolean
  time: string // HH:mm 格式
  message: string
  interval: 'daily' | 'hourly'
}

const DEFAULT_GREETING: GreetingConfig = {
  enabled: false,
  time: '09:00',
  message: '早上好呀！新的一天也要开心哦~',
  interval: 'daily',
}

export function useGreeting() {
  const config = ref<GreetingConfig>({ ...DEFAULT_GREETING })
  const { notify } = useNotification()
  let checkInterval: number | null = null
  let lastTriggerDate = ''

  // 回调函数：当触发问候时调用
  let onGreetingTriggered: ((message: string) => void) | null = null

  function setOnGreetingTriggered(callback: (message: string) => void) {
    onGreetingTriggered = callback
  }

  async function loadConfig() {
    try {
      const saved = await invoke<string | null>('get_setting', { key: 'greeting_config' })
      if (saved) {
        config.value = { ...DEFAULT_GREETING, ...JSON.parse(saved) }
      }
    } catch (e) {
      console.error('Failed to load greeting config:', e)
    }
  }

  async function saveConfig() {
    try {
      await invoke('save_setting', {
        key: 'greeting_config',
        value: JSON.stringify(config.value),
      })
    } catch (e) {
      console.error('Failed to save greeting config:', e)
    }
  }

  function updateConfig(newConfig: Partial<GreetingConfig>) {
    config.value = { ...config.value, ...newConfig }
    saveConfig()
    if (config.value.enabled) {
      startChecking()
    } else {
      stopChecking()
    }
  }

  function shouldTrigger(): boolean {
    const now = new Date()
    const today = now.toISOString().split('T')[0]
    const currentTime = now.toTimeString().slice(0, 5) // HH:mm

    if (config.value.interval === 'daily') {
      // 每天一次：检查是否到达设定时间，且今天还没触发过
      return currentTime >= config.value.time && lastTriggerDate !== today
    } else {
      // 每小时一次：检查是否到达设定分钟，且当前小时还没触发过
      const currentHour = now.getHours()
      const [targetHour, targetMinute] = config.value.time.split(':').map(Number)
      return currentHour === targetHour && currentTime.startsWith(config.value.time.slice(0, 3)) && lastTriggerDate !== `${today}-${currentHour}`
    }
  }

  function checkAndTrigger() {
    if (!config.value.enabled) return

    if (shouldTrigger()) {
      const now = new Date()
      const today = now.toISOString().split('T')[0]

      // 记录触发时间
      if (config.value.interval === 'daily') {
        lastTriggerDate = today
      } else {
        lastTriggerDate = `${today}-${now.getHours()}`
      }

      // 发送问候消息
      if (onGreetingTriggered) {
        onGreetingTriggered(config.value.message)
      }

      // 发送通知
      notify('Nova Link', config.value.message)
    }
  }

  function startChecking() {
    if (checkInterval) return
    // 每分钟检查一次
    checkInterval = window.setInterval(checkAndTrigger, 60000)
    // 立即检查一次
    checkAndTrigger()
  }

  function stopChecking() {
    if (checkInterval) {
      clearInterval(checkInterval)
      checkInterval = null
    }
  }

  function init(callback: (message: string) => void) {
    setOnGreetingTriggered(callback)
    loadConfig().then(() => {
      if (config.value.enabled) {
        startChecking()
      }
    })
  }

  onUnmounted(() => {
    stopChecking()
  })

  return {
    config,
    updateConfig,
    loadConfig,
    saveConfig,
    startChecking,
    stopChecking,
    init,
    setOnGreetingTriggered,
  }
}
```

**</action>**

**<acceptance_criteria>**
- [ ] `src/composables/useGreeting.ts` 存在
- [ ] 导出 useGreeting 函数
- [ ] 包含 enabled, time, message, interval 配置
- [ ] 定时检查逻辑正确

**</acceptance_criteria>**

---

### Task 2: 集成定时问候到 App.vue

**requirements:** GREETING-01

**files_modified:**
- `src/App.vue` (修改)

**<read_first>**
- `src/App.vue` - 查看现有结构，找到集成位置

**</read_first>**

**<action>**

修改 `src/App.vue`：

1. 导入 useGreeting：
```typescript
import { useGreeting } from './composables/useGreeting'
```

2. 在 setup 中初始化：
```typescript
// 在 setup() 中
const { init: initGreeting } = useGreeting()

// 在 onMounted 中初始化问候
onMounted(async () => {
  // ... 现有初始化代码 ...

  // 初始化定时问候，传入回调函数
  initGreeting((message: string) => {
    // 在聊天中插入问候消息
    addMessage('bot', message)
  })
})
```

**</action>**

**<acceptance_criteria>**
- [ ] App.vue 导入并初始化 useGreeting
- [ ] 定时触发时自动添加消息到聊天

**</acceptance_criteria>**

---

### Task 3: 添加定时问候设置UI

**requirements:** GREETING-01

**files_modified:**
- `src/components/CharacterSettingsModal.vue` (修改)

**<read_first>**
- `src/components/CharacterSettingsModal.vue` - 查看设置面板结构

**</read_first>**

**<action>**

在设置面板中添加"定时问候"设置区域：

1. 在 CharacterSettingsModal.vue 的 App Settings 部分添加：
```vue
<!-- 定时问候设置 -->
<div class="setting-group">
  <h3>定时问候</h3>

  <div class="setting-item">
    <label class="switch">
      <input
        type="checkbox"
        :checked="greetingConfig.enabled"
        @change="updateGreeting({ enabled: ($event.target as HTMLInputElement).checked })"
      />
      <span class="slider"></span>
    </label>
    <span>启用定时问候</span>
  </div>

  <div class="setting-item" v-if="greetingConfig.enabled">
    <label>提醒时间</label>
    <select
      :value="greetingConfig.time"
      @change="updateGreeting({ time: ($event.target as HTMLSelectElement).value })"
    >
      <option value="09:00">09:00 早上</option>
      <option value="12:00">12:00 中午</option>
      <option value="18:00">18:00 傍晚</option>
      <option value="21:00">21:00 晚上</option>
    </select>
  </div>

  <div class="setting-item" v-if="greetingConfig.enabled">
    <label>问候消息</label>
    <textarea
      :value="greetingConfig.message"
      @input="updateGreeting({ message: ($event.target as HTMLTextAreaElement).value })"
      placeholder="输入问候消息..."
      rows="3"
    ></textarea>
  </div>

  <div class="setting-item" v-if="greetingConfig.enabled">
    <label>提醒频率</label>
    <select
      :value="greetingConfig.interval"
      @change="updateGreeting({ interval: ($event.target as HTMLSelectElement).value as 'daily' | 'hourly' })"
    >
      <option value="daily">每天</option>
      <option value="hourly">每小时</option>
    </select>
  </div>
</div>
```

2. 在 script 中添加：
```typescript
import { ref } from 'vue'
import { useGreeting, type GreetingConfig } from '../composables/useGreeting'

// 在 setup() 中
const { config: greetingConfig, updateConfig: updateGreeting, loadConfig } = useGreeting()

// 加载配置
loadConfig()
```

3. 添加必要的样式（参考现有设置样式）

**</action>**

**<acceptance_criteria>**
- [ ] 设置面板中存在定时问候开关
- [ ] 可以设置提醒时间
- [ ] 可以自定义问候消息
- [ ] 可以设置提醒频率（每天/每小时）

**</acceptance_criteria>**

---

## Verification

### Must Have (for phase goal)

1. **定时提醒**: 用户可以设置定时提醒/问候
2. **主动发送**: 到设定时间时应用主动发送消息到聊天
3. **通知提醒**: 同时发送 Windows 系统通知
4. **灵活配置**: 用户可以自定义消息内容和提醒时间

---

*Plan created: 2026-03-18*
*Phase: 2 - 桌面集成 (Plan 3 of 3)*
