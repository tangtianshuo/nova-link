---
phase: 02-desktop-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/chat_history.rs
  - src-tauri/src/config.rs
  - src/composables/useChatHistory.ts
  - src/App.vue
autonomous: true
requirements:
  - HISTORY-01

must_haves:
  truths:
    - 关闭应用后聊天历史不会丢失
    - 重新打开应用可以看到之前的对话
    - 用户可以手动清除历史
  artifacts:
    - path: src-tauri/src/commands/chat_history.rs
      exports: save_chat_history_cmd, load_chat_history_cmd, clear_chat_history
    - path: src/composables/useChatHistory.ts
      exports: useChatHistory
    - path: exe/config/chat_history.json
      contains: messages array
  key_links:
    - from: App.vue
      to: useChatHistory
      via: onMounted loadHistory, onUnmounted saveHistory
---

# Plan: 02-desktop-integration

**Phase:** 2
**Plan:** 2
**Type:** execute
**Wave:** 2
**Goal:** 实现聊天历史持久化 - 关闭应用后历史可恢复

## Context

### User Decisions (from CONTEXT.md)
- Use JSON file storage in exe/config/ for persistence
- Chat messages stored in chat_history.json
- Follow existing settings storage pattern

### Must-Haves (Goal-Backward)

**Truths:**
1. 关闭应用后聊天历史不会丢失
2. 重新打开应用可以看到之前的对话
3. 用户可以手动清除历史

**Artifacts:**
- `src-tauri/src/commands/chat_history.rs` - Rust 后端聊天历史存储
- `src/composables/useChatHistory.ts` - 前端聊天历史管理
- `exe/config/chat_history.json` - 历史记录存储文件

**Key Links:**
- useChat composable -> useChatHistory -> chat_history.json
- App.vue onMounted -> loadChatHistory -> 恢复消息
- App.vue onUnmounted -> saveChatHistory -> 保存消息

---

## Tasks

### Task 1: 创建聊天历史存储命令 (Rust)

**requirements:** HISTORY-01

**files_modified:**
- `src-tauri/src/commands/mod.rs` (修改)
- `src-tauri/src/commands/chat_history.rs` (新建)
- `src-tauri/src/config.rs` (修改 - 添加聊天历史存储路径)

**<read_first>**
- `src-tauri/src/commands/settings.rs` - 参考现有存储命令
- `src-tauri/src/config.rs` - 查看现有配置存储模式

**</read_first>**

**<action>**

1. 修改 `src-tauri/src/config.rs`，添加聊天历史存储路径函数：
```rust
use serde::{Deserialize, Serialize};
use std::path::PathBuf;

#[derive(Debug, Serialize, Deserialize, Default)]
pub struct ChatHistory {
    pub messages: Vec<ChatMessage>,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct ChatMessage {
    pub msg_type: String,  // "user" or "bot"
    pub content: String,
    pub timestamp: i64,
}

pub fn get_chat_history_path() -> PathBuf {
    get_config_dir().join("chat_history.json")
}

pub fn load_chat_history() -> Result<ChatHistory, String> {
    let path = get_chat_history_path();
    if !path.exists() {
        return Ok(ChatHistory { messages: vec![] });
    }
    let content = std::fs::read_to_string(&path).map_err(|e| e.to_string())?;
    serde_json::from_str(&content).map_err(|e| e.to_string())
}

pub fn save_chat_history(history: &ChatHistory) -> Result<(), String> {
    let path = get_chat_history_path();
    let content = serde_json::to_string_pretty(history).map_err(|e| e.to_string())?;
    std::fs::write(&path, content).map_err(|e| e.to_string())
}
```

2. 创建 `src-tauri/src/commands/chat_history.rs`：
```rust
use crate::config::{load_chat_history, save_chat_history, ChatHistory, ChatMessage};

#[tauri::command]
pub fn save_chat_history_cmd(messages: Vec<ChatMessage>) -> Result<(), String> {
    let history = ChatHistory { messages };
    save_chat_history(&history)
}

#[tauri::command]
pub fn load_chat_history_cmd() -> Result<Vec<ChatMessage>, String> {
    let history = load_chat_history()?;
    Ok(history.messages)
}

#[tauri::command]
pub fn clear_chat_history() -> Result<(), String> {
    save_chat_history(&ChatHistory { messages: vec![] })
}
```

3. 修改 `src-tauri/src/commands/mod.rs`：
```rust
pub mod chat_history;
```

4. 在 `lib.rs` 的 invoke_handler 中添加：
```rust
commands::chat_history::save_chat_history_cmd,
commands::chat_history::load_chat_history_cmd,
commands::chat_history::clear_chat_history,
```

**</action>**

**<acceptance_criteria>**
- [ ] `src-tauri/src/commands/chat_history.rs` 存在
- [ ] lib.rs 注册了 save/load/clear_chat_history 命令
- [ ] 编译通过

**</acceptance_criteria>**

---

### Task 2: 创建前端聊天历史Composable

**requirements:** HISTORY-01

**files_modified:**
- `src/composables/useChatHistory.ts` (新建)

**<read_first>**
- `src/composables/useChat.ts` - 查看现有消息结构
- `src/composables/useSettings.ts` - 参考存储模式

**</read_first>**

**<action>**

创建 `src/composables/useChatHistory.ts`：
```typescript
import { ref, watch } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import type { ChatMessage } from './useChat'

export function useChatHistory() {
  const isLoading = ref(false)

  async function loadHistory(): Promise<ChatMessage[]> {
    isLoading.value = true
    try {
      const messages = await invoke<ChatMessage[]>('load_chat_history_cmd')
      return messages
    } catch (e) {
      console.error('Failed to load chat history:', e)
      return []
    } finally {
      isLoading.value = false
    }
  }

  async function saveHistory(messages: ChatMessage[]): Promise<void> {
    try {
      // 只保存最近 100 条消息
      const trimmed = messages.slice(-100)
      await invoke('save_chat_history_cmd', { messages: trimmed })
    } catch (e) {
      console.error('Failed to save chat history:', e)
    }
  }

  async function clearHistory(): Promise<void> {
    try {
      await invoke('clear_chat_history')
    } catch (e) {
      console.error('Failed to clear chat history:', e)
    }
  }

  return {
    isLoading,
    loadHistory,
    saveHistory,
    clearHistory,
  }
}
```

**</action>**

**<acceptance_criteria>**
- [ ] `src/composables/useChatHistory.ts` 存在
- [ ] 导出 loadHistory, saveHistory, clearHistory 函数
- [ ] TypeScript 编译通过

**</acceptance_criteria>**

---

### Task 3: 集成聊天历史到 App.vue

**requirements:** HISTORY-01

**files_modified:**
- `src/App.vue` (修改)

**<read_first>**
- `src/App.vue` - 查看现有结构，找到集成位置

**</read_first>**

**<action>**

修改 `src/App.vue`：

1. 导入 useChatHistory：
```typescript
import { useChatHistory } from './composables/useChatHistory'
```

2. 在组件初始化时加载历史：
```typescript
// 在 setup() 中
const { loadHistory, saveHistory } = useChatHistory()

// 在 onMounted 中，初始化完成后加载历史
onMounted(async () => {
  // ... 现有初始化代码 ...

  // 加载聊天历史
  const historyMessages = await loadHistory()
  if (historyMessages.length > 0) {
    messages.value = historyMessages
  }
})
```

3. 在组件卸载时保存历史：
```typescript
import { onUnmounted } from 'vue'

// 在 setup() 中
onUnmounted(async () => {
  // 保存聊天历史
  await saveHistory(messages.value)
})
```

4. 在消息更新时也保存（可选优化，使用防抖）：
```typescript
import { watch } from 'vue'

// 监听消息变化，自动保存
let saveTimeout: number | null = null
watch(messages, async (newMessages) => {
  if (saveTimeout) clearTimeout(saveTimeout)
  saveTimeout = window.setTimeout(() => {
    saveHistory(newMessages)
  }, 2000) // 2秒防抖
}, { deep: true })
```

**</action>**

**<acceptance_criteria>**
- [ ] App.vue 导入 useChatHistory
- [ ] onMounted 中调用 loadHistory
- [ ] onUnmounted 中调用 saveHistory
- [ ] 应用启动后历史消息被恢复

**</acceptance_criteria>**

---

## Verification

### Must Have (for phase goal)

1. **聊天历史持久化**: 关闭应用后重新打开，之前的对话记录仍然存在
2. **历史恢复**: 应用启动时自动加载并显示历史消息
3. **手动清除**: 用户可以清除历史记录

---

*Plan created: 2026-03-18*
*Phase: 2 - 桌面集成 (Plan 2 of 3)*
