---
phase: 03-sqlite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/config.rs
  - src-tauri/src/commands/chat_history.rs
  - src/composables/useChatHistory.ts
autonomous: true
requirements:
  - SQLITE-01
  - SQLITE-02
  - SQLITE-03
  - SQLITE-04
  - SQLITE-05
user_setup: []
must_haves:
  truths:
    - "聊天历史存储在 SQLite 数据库 (exe/config/chat_history.db)"
    - "现有 JSON 数据自动迁移到 SQLite"
    - "查询性能通过索引提升"
    - "SQLite 失败时自动降级到 JSON"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      contains: "rusqlite"
    - path: "src-tauri/src/config.rs"
      contains: "fn init_db"
      provides: "数据库初始化"
    - path: "src-tauri/src/config.rs"
      contains: "CREATE TABLE"
      provides: "表结构创建"
    - path: "src-tauri/src/config.rs"
      contains: "load_messages_from_db"
      provides: "消息加载"
    - path: "src-tauri/src/config.rs"
      contains: "save_messages_to_db"
      provides: "消息保存"
    - path: "src-tauri/src/config.rs"
      contains: "migrate_json_to_sqlite"
      provides: "数据迁移"
    - path: "src-tauri/src/commands/chat_history.rs"
      contains: "load_chat_history_cmd"
      provides: "Tauri命令"
  key_links:
    - from: "commands/chat_history.rs"
      to: "config.rs (SQLite functions)"
      via: "init_db(), load_messages_from_db(), save_messages_to_db()"
      pattern: "use crate::config::"
---

<objective>
将聊天历史存储从 JSON 改为 SQLite，提升性能和可扩展性

Purpose: 使用 SQLite 替代 JSON 文件存储聊天历史，支持更高效的查询、分页和未来扩展（如按会话分组）

Output: SQLite 数据库存储聊天历史，前端 composable 更新，JSON 降级方案保留
</objective>

<execution_context>
@D:\Projects\Tauri\nova-link\src-tauri\src\commands\chat_history.rs
@D:\Projects\Tauri\nova-link\src\composables\useChatHistory.ts
@D:\Projects\Tauri\nova-link\src-tauri\src\config.rs
@D:\Projects\Tauri\nova-link\src-tauri\Cargo.toml
</execution_context>

<context>
## Current Implementation

### JSON Storage (config.rs)
- ChatHistory 结构体包含 messages: Vec<ChatMessage>
- ChatMessage 包含 msg_type, content, timestamp
- 数据存储在 exe/config/chat_history.json
- load_chat_history() 和 save_chat_history() 函数

### Frontend (useChatHistory.ts)
- loadHistory() 调用 invoke('load_chat_history_cmd')
- saveHistory(messages) 调用 invoke('save_chat_history_cmd')
- clearHistory() 调用 invoke('clear_chat_history')
- 仅保留最近 100 条消息

### Existing Commands (chat_history.rs)
- save_chat_history_cmd(messages: Vec<ChatMessage>)
- load_chat_history_cmd() -> Vec<ChatMessage>
- clear_chat_history()

## Database Design

### Table: conversations
| Column | Type | Description |
|--------|------|-------------|
| id | TEXT PRIMARY KEY | 会话ID (如 "default") |
| title | TEXT | 会话标题 |
| created_at | INTEGER | 创建时间戳 |
| updated_at | INTEGER | 更新时间戳 |

### Table: messages
| Column | Type | Description |
|--------|------|-------------|
| id | INTEGER PRIMARY KEY | 自增ID |
| conversation_id | TEXT | 外键关联 conversations.id |
| role | TEXT | "user" 或 "bot" |
| content | TEXT | 消息内容 |
| timestamp | INTEGER | 时间戳 |
| created_at | INTEGER | 创建时间 |

## Key Interfaces

### Rust - New Database Functions
```rust
// 初始化数据库连接（单例）
fn get_db() -> Result<Connection, String>

// 初始化表结构
fn init_db_tables() -> Result<()>

// 从 SQLite 加载消息
fn load_messages_from_db(conversation_id: &str, limit: i64) -> Result<Vec<ChatMessage>, String>

// 保存消息到 SQLite
fn save_messages_to_db(conversation_id: &str, messages: &[ChatMessage]) -> Result<(), String>

// 迁移现有 JSON 数据
fn migrate_json_to_sqlite() -> Result<(), String>
```

### Frontend - Updated Interface
```typescript
// 新增参数支持
loadHistory(conversationId?: string, limit?: number): Promise<ChatHistoryMessage[]>
saveHistory(messages: ChatHistoryMessage[], conversationId?: string): Promise<void>
clearHistory(conversationId?: string): Promise<void>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite dependency and create database schema</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/config.rs</files>
  <read_first>
    - src-tauri/Cargo.toml (check current dependencies)
    - src-tauri/src/config.rs (existing config patterns, ChatHistory struct)
  </read_first>
  <action>
    1. Add rusqlite dependency to Cargo.toml:
       - Add `rusqlite = { version = "0.32", features = ["bundled"] }`
       - Add `thiserror = "2"` for error handling

    2. In config.rs, add new database module:
       - Add `use rusqlite::{Connection, params};`
       - Add `use thiserror::Error;`
       - Add `#[derive(Error, Debug)] pub enum DbError { ... }`

    3. Add database path function:
       ```rust
       pub fn get_db_path() -> std::path::PathBuf {
           get_config_dir().join("chat_history.db")
       }
       ```

    4. Add database initialization function:
       ```rust
       pub fn init_db() -> Result<Connection, String> {
           let db_path = get_db_path();
           let conn = Connection::open(&db_path).map_err(|e| e.to_string())?;

           // Create tables if not exist
           conn.execute(
               "CREATE TABLE IF NOT EXISTS conversations (
                   id TEXT PRIMARY KEY,
                   title TEXT NOT NULL DEFAULT '',
                   created_at INTEGER NOT NULL,
                   updated_at INTEGER NOT NULL
               )",
               [],
           ).map_err(|e| e.to_string())?;

           conn.execute(
               "CREATE TABLE IF NOT EXISTS messages (
                   id INTEGER PRIMARY KEY AUTOINCREMENT,
                   conversation_id TEXT NOT NULL,
                   role TEXT NOT NULL,
                   content TEXT NOT NULL,
                   timestamp INTEGER NOT NULL,
                   created_at INTEGER NOT NULL,
                   FOREIGN KEY (conversation_id) REFERENCES conversations(id)
               )",
               [],
           ).map_err(|e| e.to_string())?;

           // Create index for faster queries
           conn.execute(
               "CREATE INDEX IF NOT EXISTS idx_messages_conversation ON messages(conversation_id)",
               [],
           ).map_err(|e| e.to_string())?;

           Ok(conn)
       }
       ```

    5. Add JSON fallback path functions (保留现有功能):
       - Keep `get_chat_history_path()` returning PathBuf to chat_history.json
       - Keep `load_chat_history()` and `save_chat_history()`
  </action>
  <verify>
    <automated>grep -n "rusqlite" src-tauri/Cargo.toml && grep -n "init_db" src-tauri/src/config.rs</automated>
  </verify>
  <done>
    - rusqlite dependency added to Cargo.toml
    - Database initialization function created in config.rs
    - Tables: conversations, messages created
    - Index on conversation_id for query performance
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement SQLite database operations and migration</name>
  <files>src-tauri/src/config.rs, src-tauri/src/commands/chat_history.rs</files>
  <read_first>
    - src-tauri/src/config.rs (Step 1 implementation)
    - src-tauri/src/commands/chat_history.rs (existing commands)
  </read_first>
  <action>
    1. In config.rs, add database operation functions:

       ```rust
       /// Load messages from SQLite (支持分页)
       pub fn load_messages_from_db(conversation_id: &str, limit: i64) -> Result<Vec<ChatMessage>, String> {
           let conn = init_db()?;

           let mut stmt = conn.prepare(
               "SELECT role, content, timestamp FROM messages
                WHERE conversation_id = ?1
                ORDER BY timestamp DESC
                LIMIT ?2"
           ).map_err(|e| e.to_string())?;

           let messages: Vec<ChatMessage> = stmt.query_map(params![conversation_id, limit], |row| {
               Ok(ChatMessage {
                   msg_type: row.get(0)?,
                   content: row.get(1)?,
                   timestamp: row.get(2)?,
               })
           }).map_err(|e| e.to_string())?
           .filter_map(|r| r.ok())
           .collect();

           // Return in chronological order (oldest first)
           Ok(messages.into_iter().rev().collect())
       }

       /// Save messages to SQLite (替换整个会话消息)
       pub fn save_messages_to_db(conversation_id: &str, messages: &[ChatMessage]) -> Result<(), String> {
           let conn = init_db()?;

           // Start transaction
           conn.execute("BEGIN TRANSACTION", [])
               .map_err(|e| e.to_string())?;

           // Delete existing messages for this conversation
           conn.execute("DELETE FROM messages WHERE conversation_id = ?1", params![conversation_id])
               .map_err(|e| e.to_string())?;

           // Insert new messages
           let now = std::time::SystemTime::now()
               .duration_since(std::time::UNIX_EPOCH)
               .unwrap()
               .as_secs() as i64;

           for msg in messages {
               conn.execute(
                   "INSERT INTO messages (conversation_id, role, content, timestamp, created_at) VALUES (?1, ?2, ?3, ?4, ?5)",
                   params![conversation_id, msg.msg_type, msg.content, msg.timestamp, now]
               ).map_err(|e| e.to_string())?;
           }

           // Update conversation timestamp
           conn.execute(
               "INSERT OR REPLACE INTO conversations (id, title, updated_at, created_at) VALUES (?1, '', ?2, ?2)",
               params![conversation_id, now]
           ).map_err(|e| e.to_string())?;

           conn.execute("COMMIT", []).map_err(|e| e.to_string())?;

           Ok(())
       }

       /// Clear messages for a conversation
       pub fn clear_messages_from_db(conversation_id: &str) -> Result<(), String> {
           let conn = init_db()?;
           conn.execute("DELETE FROM messages WHERE conversation_id = ?1", params![conversation_id])
               .map_err(|e| e.to_string())?;
           Ok(())
       }

       /// Migrate existing JSON data to SQLite (if SQLite is empty)
       pub fn migrate_json_to_sqlite() -> Result<(), String> {
           let conn = init_db()?;

           // Check if already migrated
           let count: i64 = conn.query_row(
               "SELECT COUNT(*) FROM messages",
               [],
               |row| row.get(0)
           ).unwrap_or(0);

           if count > 0 {
               return Ok(()); // Already has data
           }

           // Load from JSON
           let json_history = load_chat_history()?;
           if json_history.messages.is_empty() {
               return Ok(());
           }

           // Migrate to SQLite
           save_messages_to_db("default", &json_history.messages)?;

           Ok(())
       }
       ```

    2. Update chat_history.rs commands to use SQLite with JSON fallback:

       ```rust
       use crate::config::{
           load_chat_history, save_chat_history, ChatHistory, ChatMessage,
           load_messages_from_db, save_messages_to_db, clear_messages_from_db,
           migrate_json_to_sqlite, init_db
       };

       #[tauri::command]
       pub fn save_chat_history_cmd(messages: Vec<ChatMessage>) -> Result<(), String> {
           // Try SQLite first, fallback to JSON
           match init_db() {
               Ok(_) => {
                   match save_messages_to_db("default", &messages) {
                       Ok(_) => return Ok(()),
                       Err(e) => {
                           log::warn!("SQLite save failed, falling back to JSON: {}", e);
                       }
                   }
               }
               Err(e) => {
                   log::warn!("SQLite init failed, using JSON: {}", e);
               }
           }

           // Fallback to JSON
           let history = ChatHistory { messages };
           save_chat_history(&history)
       }

       #[tauri::command]
       pub fn load_chat_history_cmd() -> Result<Vec<ChatMessage>, String> {
           // Try SQLite first
           match init_db() {
               Ok(_) => {
                   match load_messages_from_db("default", 100) {
                       Ok(messages) if !messages.is_empty() => return Ok(messages),
                       Ok(_) => {
                           // SQLite empty, try migration
                           if let Err(e) = migrate_json_to_sqlite() {
                               log::warn!("Migration failed: {}", e);
                           }
                           // Try again after migration
                           if let Ok(messages) = load_messages_from_db("default", 100) {
                               if !messages.is_empty() {
                                   return Ok(messages);
                               }
                           }
                       }
                       Err(e) => {
                           log::warn!("SQLite load failed: {}", e);
                       }
                   }
               }
               Err(e) => {
                   log::warn!("SQLite init failed: {}", e);
               }
           }

           // Fallback to JSON
           let history = load_chat_history()?;
           Ok(history.messages)
       }

       #[tauri::command]
       pub fn clear_chat_history() -> Result<(), String> {
           // Try SQLite first
           match init_db() {
               Ok(_) => {
                   match clear_messages_from_db("default") {
                       Ok(_) => return Ok(()),
                       Err(e) => {
                           log::warn!("SQLite clear failed, using JSON: {}", e);
                       }
                   }
               }
               Err(e) => {
                   log::warn!("SQLite init failed: {}", e);
               }
           }

           // Fallback to JSON
           save_chat_history(&ChatHistory { messages: vec![] })
       }
       ```

    3. Add migration on startup (在 lib.rs 中应用初始化时调用):
       - 在应用启动时调用 `migrate_json_to_sqlite()` 以自动迁移现有数据
  </action>
  <verify>
    <automated>grep -n "load_messages_from_db\|save_messages_to_db\|migrate_json_to_sqlite" src-tauri/src/config.rs</automated>
  </verify>
  <done>
    - load_messages_from_db function implemented
    - save_messages_to_db function implemented
    - clear_messages_from_db function implemented
    - migrate_json_to_sqlite function implemented
    - Commands updated with SQLite-first + JSON fallback
    - Auto-migration on startup
  </done>
</task>

<task type="auto">
  <name>Task 3: Update frontend composable with new features</name>
  <files>src/composables/useChatHistory.ts</files>
  <read_first>
    - src/composables/useChatHistory.ts (existing implementation)
  </read_first>
  <action>
    Update useChatHistory.ts to optionally support conversation IDs:

    ```typescript
    import { ref } from 'vue'
    import { invoke } from '@tauri-apps/api/core'

    export interface ChatHistoryMessage {
      msg_type: 'user' | 'bot'
      content: string
      timestamp: number
    }

    export function useChatHistory() {
      const isLoading = ref(false)

      async function loadHistory(conversationId?: string): Promise<ChatHistoryMessage[]> {
        isLoading.value = true
        try {
          // Backend handles SQLite/JSON automatically
          const messages = await invoke<ChatHistoryMessage[]>('load_chat_history_cmd')
          return messages
        } catch (e) {
          console.error('Failed to load chat history:', e)
          return []
        } finally {
          isLoading.value = false
        }
      }

      async function saveHistory(messages: ChatHistoryMessage[]): Promise<void> {
        try {
          // Only save last 100 messages
          const trimmed = messages.slice(-100)
          await invoke('save_chat_history_cmd', { messages: trimmed })
        } catch (e) {
          console.error('Failed to save chat history:', e)
        }
      }

      async function clearHistory(): Promise<void> {
        try {
          await invoke('clear_chat_history')
        } catch (e) {
          console.error('Failed to clear chat history:', e)
        }
      }

      return {
        isLoading,
        loadHistory,
        saveHistory,
        clearHistory,
      }
    }
    ```

    Note: Frontend interface remains compatible - the conversationId parameter can be added later when multi-conversation support is needed. Backend handles all SQLite/JSON logic transparently.
  </action>
  <verify>
    <automated>grep -n "loadHistory\|saveHistory\|clearHistory" src/composables/useChatHistory.ts</automated>
  </verify>
  <done>
    - Frontend composable exports remain the same
    - Backend transparently handles SQLite/JSON
    - Compatible with existing chat panel integration
  </done>
</task>

</tasks>

<verification>
## Phase Verification

After implementation:

1. **SQLite Storage**: Check that exe/config/chat_history.db exists after running
2. **Migration**: Existing JSON data automatically migrated on first run
3. **Query Performance**: Messages load faster with indexed queries
4. **Fallback**: If SQLite fails, JSON storage still works

Test commands:
```bash
# Check SQLite database exists
ls exe/config/chat_history.db

# Check tables created
sqlite3 exe/config/chat_history.db ".tables"

# Check messages stored
sqlite3 exe/config/chat_history.db "SELECT * FROM messages LIMIT 5;"
```
</verification>

<success_criteria>
- [x] 聊天历史使用 SQLite 存储 (SQLite-first, JSON fallback)
- [x] 现有 JSON 数据平滑迁移 (auto-migration on first run)
- [x] 查询性能提升 (indexed by conversation_id)
- [x] 向后兼容 (JSON fallback if SQLite fails)
</success_criteria>

<output>
After completion, create `.planning/phases/03-sqlite/03-sqlite-01-SUMMARY.md`
</output>
